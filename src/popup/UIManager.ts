// src/popup/UIManager.ts
export class UIManager {
  displayKeywords(keywords: string[]): void {
    // Similar to the provided displayKeywords function
  }

  displayDomains(domains: string[]): void {
    // Similar to the provided displayDomains function
  }

  updateKeywordsUI(): void {
    // Update the UI to reflect the new keywords
  }

  updateDomainsUI(): void {
    // Update the UI to reflect the new domains
  }
}
